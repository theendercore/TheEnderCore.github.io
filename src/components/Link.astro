---
export interface Props {
  title: string;
  href: string;
}

const { href, title } = Astro.props;
---

<span>
  <a href={href} class="text-purple-400 font-bold hover:scale-125">{title}</a>
</span>
